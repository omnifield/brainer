# frontend — control-panel дашборд

Тот самый недостающий интерфейс: пульт над agent-сессиями. **Первый билд продукта**
(contract-first, против мок-контракта). Временный-но-чистый; реальный придёт из
фреймворка позже. Скоуп MVP — `briefs/interface-mvp.md`. Реализация — по брифу.
