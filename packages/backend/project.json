{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@omnifield/brainer-backend",
  "projectType": "application",
  "targets": {
    "test:py": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run pytest -q",
        "cwd": "{projectRoot}"
      }
    }
  }
}
