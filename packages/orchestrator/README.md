# orchestrator — lifecycle сессий + провайдеры + телеметрия

Запуск/стоп agent-сессий (провайдер `claude-code` = claude-scope процесс), реестр провайдеров,
**агрегация OTEL-телеметрии** (scope/промпты/tool-детали уже текут в collector :4317).
Зависит максимум от `kernel`-контрактов. Переедет в `engines`. Реализация — по брифу.
