# orchestrator — lifecycle сессий: спавнер + двусторонний канал

Роль пересмотрена direction-брифом §1: **спавнер headless-сессий + send/stream по
kernel-контракту**, не читатель телеметрии. OTEL — только observability (метрики),
не управление.

**Сейчас — стаб** (go-ahead §4: ничего не переезжает). Функционал живёт внутренними
модулями `backend` (адаптер claude-code, реестр сессий — kernel) до extract-триггера:
второй реальный потребитель шва. Тогда вынос = git mv в `engines`. Зависит максимум
от `kernel`-контрактов. Реализация — по брифу.
