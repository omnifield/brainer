# Review — frontend chat view (`eaf69fa`) + backend fixup (`8ef81f2`), вердикт architect

| | |
|---|---|
| **К брифам** | `control-channel-frontend.md`; fixup — по `review-control-channel-backend.md` |
| **От** | brainer-архитектор, 2026-07-09 |
| **Вердикт** | backend-fixup: **ПРИНЯТ, шаг 2 закрыт**. frontend: **APPROVE с fixup'ами** — Ф1 обязателен; Ф2 — контрактное решение, парный фикс backend+frontend; Ф3 — минор |

## Backend-fixup `8ef81f2` — принят без замечаний

П1–П3 ревью закрыты как предписано; вынос seq-эпох в `channel/seq.py` (delivery-слой,
адаптер ре-экспортирует) — лучше предложенного, hub больше не тянет константу из
конкретного провайдера. +2 юнита ровно на пути ревью. 35/35, ruff чист.

## Frontend — что проверено

- Юниты 51/51, biome, typecheck, build (с drift-гейтом `gen:types --check`) — прогнано
  архитектором. Кодген из kernel-схемы честный: ноль ручных доменных типов, разъезд
  ломает build + юнит.
- Миграционная секция брифа соблюдена: редьюсер — чистый TS без DOM (`store/chat/reducer.ts`),
  каждый тип события — props-only компонент (`components/chat/*`), экран — только композиция,
  прото-токены `--chat-*` в `index.css`, side-effects только в `api/`.
- **Живой e2e прогнан архитектором** (то, что owner отложил): бэк :8000 + vite :5173,
  launch haiku-сессии из UI (scope kernel) → SSE-лента живая (status/hook-события,
  свёрнутый thinking, реплика агента, done-строка с usage/cost) → send из чата → ответ
  в том же треде → client-side повторный заход → лента восстановилась полностью, без
  дублей → stop. Скриншоты в сессии ревью.

## Ф1 (баг, обязателен): hard-refresh на `/sessions/:id` отдаёт голый 404-JSON бэка

Vite-прокси `"/sessions" → :8000` заглатывает и **HTML-навигацию** SPA-роута
`/sessions/:id`: FastAPI отвечает `{"detail":"Not Found"}`, приложение умирает.
DoD-пункт «refresh → лента восстановилась» не выполняется буквально.

Корень: коллизия неймспейса SPA-роута и API-поверхности за одним origin. Контракт ручек
(§1.5) НЕ меняем. Фикс на стороне фронта, механизм на выбор owner: `bypass` в vite-proxy
(проксировать только не-HTML Accept) или увести SPA-роут с `/sessions/*` (например
`/s/:id`). Тот же выбор обязан работать в prod-раскладке за одним origin — задокументируй.

## Ф2 (контракт, ратифицировано): user-реплика обязана жить в wire, не только оптимистично

Сейчас echo юзера — только optimistic-локально; после reconnect/replay реплика юзера
**исчезает из ленты** (провайдер её не эмиттит). Словарь контракта уже имеет
`message {role: user}` — резерв используем:

- **backend** (мелкий fixup): адаптер на `send()` эмиттит `message {role: user, text}`
  в стрим сессии (seq — его же, порядок «user-реплика до ответа» гарантирован местом
  инжекта). Попадает в ring-буфер → replay честный.
- **frontend**: при приходе wire `message {role:user}` вытеснять соответствующий
  optimistic-элемент (механизм дедупа — на owner), рендер уже готов (role из payload).

## Ф3 (минор): статус в шапке сессии не обновляется по `done`

Список (проекция бэка) после `done` показывает Waiting, шапка чата — Running до
следующего `status`-события. Редьюсер: `done` → `sessionState = reason === "stopped"
? "stopped" : "waiting"` — зеркало серверной проекции.

## Принято к сведению

- pnpm-lock.yaml в корне — легитимное следствие devDep кодгена, не out-of-zone правка.
- biome: 11 warnings — не гейт, но глянуть при Ф-фиксапе.

## Закрытие (2026-07-09, после fixup'ов `1027e27` + `9ddb156`)

**Ф1–Ф3 закрыты, шаг 3 закрыт.** Проверено архитектором, не со слов:

- Гейты: backend 36/36 + ruff; frontend 55/55 + build (drift-гейт). 
- Ф1 вживую: `GET /s/:id` → 200 HTML (приложение), `/sessions` → проксируется в API.
  Структурный развод неймспейсов (роут `/s/:id`) — правильнее Accept-сниффинга,
  держится и в prod-раскладке.
- Ф2 вживую (API-прогон, haiku): echo `message {role:user}` в стриме на seq ДО
  ответа агента, и приходит из ring-буфера при подключении после send — реплика
  в wire, replay честный. Merge-lane адаптера (seq на dequeue, FIFO == seq-порядок) —
  корректное владение seq.
- Ф3: `done` → `sessionState waiting|stopped`, зеркало серверной проекции.

Заметки в бэклог следующего захода в backend (НЕ блокируют):

1. `_pump_sdk` ловит только `ClaudeSDKError`: не-SDK исключение в pump'е закроет
   стрим молча (finally → closed) без `error`-события — раньше hub ловил это как
   `stream_crash`. Расширить до `Exception` → `("error", exc)`.
2. `send()` инжектит echo до `client.query()`: если query упадёт, в ленте останется
   «отправленная» реплика без хода. Порядок оставить (гарантия «echo до ответа»),
   но при исключении query — эмиттить `error` следом.

## После fixup'ов

Шаг 3 закрыт → готов догфуд-смок (мы сами в чате вместо терминала). Параллельный трек
`presets-backend.md` не заблокирован; owner-kernel при ближайшем заходе забирает
`current_handle` в контракт (П2 прошлого ревью) — Ф2-инжект user-реплики его не ждёт.
